# Core
torch
transformers
accelerate
huggingface_hub
datasets

# Numerics / stats
numpy
pandas
scipy
scikit-learn

# Interpretability (hooks, patching, steering, SAEs)
transformer-lens
sae-lens
einops

# Config / utils / plots
pyyaml
tqdm
matplotlib

# S2 vignette rendering (paraphrase backends optional; install the one you use)
datasketch                 # MinHash near-dup QC
# google-generativeai      # Gemini Flash paraphrase backend
# openai                   # GPT-4o-mini paraphrase backend

# Testing
pytest
